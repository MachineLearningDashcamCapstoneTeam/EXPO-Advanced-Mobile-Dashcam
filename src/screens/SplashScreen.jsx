
import * as React from 'react';
import { View, Text } from 'react-native';

function SplashScreen() {
    return (
      <View>
        <Text>Loading...</Text>
      </View>
    );
  
    }

    export default SplashScreen;

    num = int (input("Enter number to test even or odd: "))
    if (num % 2 ) == 0:
      print("Number is even")
    else:
      print ("Number is odd")